import { makeStyles } from "@material-ui/core/styles";

export const useStyle = makeStyles({
  box: {
    textAlign: "center",
  },
  list: {
    display: "flex",
    flexDirection: "row",
  },
  listItem: {
    textAlign: "center",
  },
});
